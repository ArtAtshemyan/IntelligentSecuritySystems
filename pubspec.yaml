name: intelligent_security_systems
description: "Intelligent Security Systems."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  analyzer: '6.4.0'
  cupertino_icons: ^1.0.8
  dio: ^5.7.0
  logger: ^2.4.0
  flutter_bloc: ^8.1.6
  hydrated_bloc: ^9.1.5
  path_provider: ^2.1.4
  get_it: ^7.7.0
  dartz: ^0.10.1
  flutter_svg: ^2.0.10+1
  mask_text_input_formatter: ^2.9.0
  pin_code_fields: ^8.0.1
  device_info_plus: ^11.1.0
  android_id: ^0.4.0
  shared_preferences: ^2.3.2
  persistent_bottom_nav_bar: ^6.2.1
  flutter_dotenv: ^5.2.1
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  googleapis_auth: ^1.6.0
  app_settings: ^5.1.1
  flutter_local_notifications: ^17.2.4


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
#    - assets/fonts/
    - assets/images/
    - assets/vectors/
    - .env
flutter_intl:
  enabled: true